---
const { currentPath = '/' } = Astro.props;

const navItems = [
  { name: 'Dashboard', path: '/', icon: '📊' },
  { name: 'Clientes', path: '/clientes', icon: '👥' },
  { name: 'Facturas', path: '/facturas', icon: '📄' },
  { name: 'Configuración', path: '/configuracion', icon: '⚙️' },
];
---


<nav class="bg-gradient-to-br from-primary-dark to-primary border-b border-white/10 shadow-lg sticky top-0 z-[100]">
  <div class="max-w-[1280px] mx-auto px-6 py-4 flex flex-col md:flex-row justify-between items-center gap-4 md:gap-0">
    <div class="flex items-center gap-4 text-white font-semibold text-xl">
      <span class="text-[1.75rem]">💼</span>
      <span class="text-base md:text-xl">Facturador Digital</span>
    </div>
    
    <ul class="flex list-none gap-2 w-full md:w-auto overflow-x-auto">
      {navItems.map(item => (
        <li>
          <a 
            href={item.path} 
            class:list={[
              'flex items-center gap-2 px-4 md:px-6 py-2 rounded-md transition-all font-medium text-sm md:text-base whitespace-nowrap',
              currentPath === item.path 
                ? 'bg-white/20 text-white' 
                : 'text-white/80 hover:bg-white/10 hover:text-white'
            ]}
          >
            <span class="text-xl">{item.icon}</span>
            <span>{item.name}</span>
          </a>
        </li>
      ))}
    </ul>
  </div>
</nav>

<style>
  /* Scoped styles removed in favor of Tailwind utility classes */
</style>
